name: scholarrag
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - fastapi
      - uvicorn[standard]
      - sqlalchemy
      - psycopg2-binary
      - pgvector
      - python-dotenv
      - requests
      - openai
      - pydantic
      - PyPDF2
      - pytest
      - flake8