from typing import Dict, List, Optional


def fetch_candidates_from_openalex(query: str, limit: int = 300, year_from: Optional[int] = None, year_to: Optional[int] = None) -> List[Dict]:
    """
    Placeholder for OpenAlex API fetch. Returns empty list for now.
    Wire actual API calls later with rate limiting and field selection.
    """
    return []

